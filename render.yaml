services:
  - type: web
    name: cricket-voting-app
    env: node
    buildCommand: npm install
    startCommand: node server.js
    disk:
      name: cricket-data
      mountPath: /data
      sizeGB: 1
    envVars:
      - key: DATA_DIR
        value: /data
      - key: NODE_ENV
        value: production
